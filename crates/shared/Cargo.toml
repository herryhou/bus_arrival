[package]
name = "shared"
version.workspace = true
edition.workspace = true

[features]
default = []
std = ["serde"]
serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, optional = true }

[dev-dependencies]
crc32fast = "1.4"
