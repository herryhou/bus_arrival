[package]
name = "shared"
version.workspace = true
edition.workspace = true

[features]
default = ["std"]
std = ["crc32fast/std", "serde"]

[dependencies]
serde = { workspace = true, optional = true, features = ["derive"] }
crc32fast = { workspace = true }
