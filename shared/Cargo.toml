[package]
name = "shared"
version.workspace = true
edition.workspace = true

[features]
default = ["std"]
std = ["crc32fast/std"]

[dependencies]
crc32fast = { workspace = true, default-features = false }
