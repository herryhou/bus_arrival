file: preprocessor/src/pack.rs
these 2 lines should be removed, due to origin is fixed to lat/lng as FIXED_ORIGIN_LAT_DEG(20.0)/FIXED_ORIGIN_LON_DEG(120.0)
    // [9-12]  Grid origin X (cm) as i32
    // [13-16] Grid origin Y (cm) as i32